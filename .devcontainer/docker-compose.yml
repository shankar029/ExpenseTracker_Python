version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspace:cached
    command: sleep infinity
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=dev-secret-key-change-in-production
      - JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production
      - DATABASE_URL=sqlite:///expense_tracker.db
      - CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://0.0.0.0:3000
    ports:
      - "3000:3000"
      - "5000:5000"
    networks:
      - expense-tracker-network
    working_dir: /workspace
    user: vscode

  # Optional: Separate services for development (commented out by default)
  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   volumes:
  #     - ../..:/workspace:cached
  #   command: bash -c "cd /workspace/backend && python run.py"
  #   environment:
  #     - FLASK_ENV=development
  #     - SECRET_KEY=dev-secret-key-change-in-production
  #     - JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production
  #     - DATABASE_URL=sqlite:///expense_tracker.db
  #     - CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://0.0.0.0:3000
  #   ports:
  #     - "5000:5000"
  #   networks:
  #     - expense-tracker-network
  #   depends_on:
  #     - app

  # frontend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   volumes:
  #     - ../..:/workspace:cached
  #   command: bash -c "cd /workspace/frontend && npm start"
  #   environment:
  #     - REACT_APP_API_URL=http://localhost:5000
  #     - CHOKIDAR_USEPOLLING=true
  #     - WATCHPACK_POLLING=true
  #   ports:
  #     - "3000:3000"
  #   networks:
  #     - expense-tracker-network
  #   depends_on:
  #     - backend

networks:
  expense-tracker-network:
    driver: bridge